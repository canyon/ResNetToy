import argparse

"""
Here are the param for the training

"""


def get_args():
    """
    Parse input arguments
    """
    parser = argparse.ArgumentParser(description='Train a ResNet network')
    # parser.add_argument('--', type=, default=, help='the number of the episodes in one epoch')
    # parser.add_argument('--', type=, default=, help='the number of the epoch to train the agent')
    # parser.add_argument('--', type=, default=, help='the number of the epoch to evaluate the agent')
    # parser.add_argument('--', type=, default=, help='how many times to train the agent in on epoch')
    # parser.add_argument('--', type=, default=, help='how often to save the model, from the train_steps perspect')
    # parser.add_argument('--', type=, default=, help='how often to update the target_net')
    # parser.add_argument('--', type=, default=, help='how often to update the target_net')
    # parser.add_argument('--', type=, default=, help='the threshold to judge whether win')
    # parser.add_argument('--', type=, default=, help='the number of the episodes in one epoch')
    # parser.add_argument('--', type=, default=, help='the number of the epoch to train the agent')
    # parser.add_argument('--', type=, default=, help='the number of the epoch to evaluate the agent')
    # parser.add_argument('--', type=, default=, help='how many times to train the agent in on epoch')
    # parser.add_argument('--', type=, default=, help='how often to save the model, from the train_steps perspect')
    # parser.add_argument('--', type=, default=, help='how often to update the target_net')
    # parser.add_argument('--', type=, default=, help='how often to update the target_net')
    # parser.add_argument('--', type=, default=, help='the threshold to judge whether win')
    args = parser.parse_args()

    return args
